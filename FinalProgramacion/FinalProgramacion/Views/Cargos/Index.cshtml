@model IEnumerable<FinalProgramacion.App_Start.Models.Cargos>

    <link href="~/Content/estilo.css" rel="stylesheet" type="text/css" />
<h1 id="Ccrear">Lista</h1>

<p>
    <br />
    <center>@Html.ActionLink("Crear", "Create")</center>
</p>
<table class="table">
    <tr>
        <th>
            <div id="ce1"> @Html.DisplayNameFor(model => model.Cargo)</div>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cargo)
        </td>
        <td id="bot">
            @Html.ActionLink("Editar", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Id }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
